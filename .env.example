# Anthropic API key for Claude calls (with web search)
ANTHROPIC_API_KEY=sk-ant-...

# PostgreSQL connection string (auto-injected by Railway Postgres plugin)
DATABASE_URL=postgresql://user:password@localhost:5432/stvnstjnl

# Shared secret to protect the /api/refresh endpoint
REFRESH_SECRET=your-secret-token-here

# Public URL of the deployed app (used by cron service)
APP_URL=http://localhost:3000
